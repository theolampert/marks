{$PROXY_HOST} {

  jwt {
    path /
    allow sub {$USERNAME}
  }

  login / {
    simple {$USERNAME}={$PASSWORD}
  }

  proxy / web:5000 {
    transparent
    policy round_robin
  }

  push /static/style.css

  cache
  minify
  gzip
  tls {$PROXY_TLS}

}

